<h2>Lista de categorias:</h2>
<hr />
<a href="/admin/categorias/add"><button class="btn btn-success mb-4">Criar nova
    categoria</button></a>

{{#each categorias}}
  <div class="card mb-2 p-2 shadow rounded border border-success-subtle ">
    <div class="card-body d-flex flex-column ">
    
        <h4>{{nome}}</h4>
        <small>Slug: {{slug}}</small>
       
        <small style="color: #098b09;">Data de criação:
          {{formatDate date}}</small>

        <div class="mt-2">
            <a href="/admin/categorias/edit/{{_id}}"><button
                type="button"
                class="btn btn-primary m-1"
              >Editar</button></a>
            <a href="/admin/categorias/deletar/{{_id}}"><button
                class="btn btn-outline-danger"
              >🗑</button></a>
        </div>
    </div>

  </div>
{{else}}
  <h5 class="mt-4" style="color: #c12c2c;">Nenhuma categoria registrada.</h5>

{{/each}}