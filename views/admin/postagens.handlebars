<h2>Lista de postagens:</h2>
<hr>
<a href="/admin/postagens/add"><button class="btn btn-success mb-4">Criar nova postagem</button></a>


{{#each postagens}}

<div class="card mb-2 p-2 shadow rounded border border-success-subtle">
    <div class="card-body d-flex flex-column">
        <h4>{{titulo}}</h4>
        <p>{{descricao}}</p>
        <small> Categoria: {{categoria.nome}}</small> 
        <small>Slug: {{slug}}</small> 
        <p style="font-weight: 500;" class="py-1">Conteúdo: {{conteudo}}</p>
        <small style="color: #2aa326;">Data: {{formatDate data}}</small>
    </div>

        <div class="mt-2 p-2">
            <a href="/admin/postagens/edit/{{_id}}"><button
                type="button"
                class="btn btn-primary"
              >Editar</button></a>
              
            <a href="/admin/postagens/deletar/{{_id}}"><button
                class="btn btn-outline-danger"
              >🗑</button></a>
        </div>
</div>


{{else}}
<p class="alert alert-danger rounded">Nenhuma postagem resgistrada!</p>
{{/each}}